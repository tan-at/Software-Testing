/*
const chai = require("chai");
const expect = chai.expect;
const equalObjects = require("../../src/.internal/equalObjects.js");

describe("equalObjects", function () {
  it("should return true for two empty objects", function () {
    const obj1 = {};
    const obj2 = {};
    expect(equalObjects(obj1, obj2)).to.be.true;
  });

  it("should return false for objects with different properties", function () {
    const obj1 = { a: 1 };
    const obj2 = { b: 1 };
    expect(equalObjects(obj1, obj2)).to.be.false;
  });

  it("should return true for identical objects", function () {
    const obj1 = { a: 1, b: { c: 2 } };
    const obj2 = { a: 1, b: { c: 2 } };
    expect(equalObjects(obj1, obj2)).to.be.true;
  });
});
*/

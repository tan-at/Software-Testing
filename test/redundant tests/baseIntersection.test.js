/*
import { expect } from "chai";
import baseIntersection from "../../src/.internal/baseIntersection";

describe("baseIntersection", () => {
  it("should return the intersection of arrays", () => {
    const arrays = [
      [1, 2, 3],
      [2, 3, 4],
      [3, 4, 5],
    ];

    const result = baseIntersection(arrays);

    expect(result).to.deep.equal([3]);
  });

  it("should handle empty arrays", () => {
    const arrays = [[1, 2, 3], [], [4, 5, 6]];

    const result = baseIntersection(arrays);

    expect(result).to.deep.equal([]);
  });

  it("should handle arrays with objects and custom iteratee", () => {
    const arrays = [
      [{ x: 1 }, { x: 2 }, { x: 3 }],
      [{ x: 2 }, { x: 3 }, { x: 4 }],
      [{ x: 3 }, { x: 4 }, { x: 5 }],
    ];

    const result = baseIntersection(arrays, (obj) => obj.x);

    expect(result).to.deep.equal([{ x: 3 }]);
  });
});
*/
